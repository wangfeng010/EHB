% 定义目标函数
fun = @RC_iden_demo02;

% 初始迭代位置
x0 = [0, 150,52,-5, 0, 3,8,0, -4, 0.4,0,0];

% 线性约束（这里不需要）
A = [];
b = [];
Aeq = [];
beq = [];

% 参数的下界和上界
lb = [0,0,0,-5,-5,0,0,0,-10,-5,0,0];
ub = [1,300,100,20,10,10,10,10,5,5,5,20];
    
% 非线性约束（这里不需要）
nonlcon = [];

% 调用fmincon函数进行优化
[V_optimized, J_min] = fmincon(fun, x0, A, b, Aeq, beq, lb, ub, nonlcon);